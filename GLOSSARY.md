# Глосарій

Єдиний словник термінів для всього репозиторію. Його дотримано в усіх статтях,
тож зміна термінології починається тут, а вже потім іде у тексти.

## Головні принципи

1. **Усталений англійський термін не перекладено.** Український розробник каже
   «embedding», «rerank», «chunking», а не «вкладення» чи «перерангування».
   Штучний переклад робить текст нечитабельним і не гуглиться.
2. **Перекладено те, що має живий український відповідник:** «затримка», «пропускна
   здатність», «права доступу», «галюцинації».
3. **При першій згадці в статті** — український опис + англійський термін у дужках:
   «повторне ранжування (rerank)». Далі по тексту — тільки англійський.
4. **Англійські терміни відмінено як українські слова:** «у промпті», «з embedding'ом»,
   «кілька чанків», «через rerank». Апостроф перед закінченням стоїть лише там, де
   без нього читається неоднозначно.
5. **Код, назви моделей, API-параметри й назви метрик не змінювано взагалі.**

---

## Базові поняття

| Оригінал (кит.) | Українською | Нотатки |
|---|---|---|
| 大模型 | велика мовна модель, LLM | у заголовках — «LLM»; у тексті чергувати |
| 大模型应用开发 | розробка застосунків на LLM | не «LLM-розробка» — це інша професія |
| 上下文 | контекст | |
| 上下文窗口 | контекстне вікно | |
| 上下文工程 | context engineering | усталене, не перекладено |
| 提示词 / 提示工程 | промпт / prompt engineering | не «підказка» |
| 系统提示词 | системний промпт | |
| 幻觉 | галюцинації | |
| 训练 | навчання | 预训练 → попереднє навчання (pre-training) |
| 推理 | інференс | 推理 = inference, а не «міркування» |
| 参数 | параметри | 参数量 → кількість параметрів |
| 分词 / 词表 | токенізація / словник токенів | |
| 涌现 | емерджентні здібності | |

## RAG

| Оригінал | Українською | Нотатки |
|---|---|---|
| 检索 | пошук (retrieval) | процес добування документів |
| 召回 (стадія) | етап відбору кандидатів | **не плутати з метрикою** |
| 召回率 (метрика) | recall | метрика — завжди англійською |
| 精准率 | precision | |
| 切片 / 分块 | чанкінг, чанк (chunk) | «розбиття на чанки» |
| 重排 / 精排 | rerank, повторне ранжування | |
| 向量数据库 | векторна база даних | |
| 向量检索 | векторний пошук | |
| 混合检索 | гібридний пошук | |
| 稠密 / 稀疏检索 | щільний / розріджений пошук | dense / sparse |
| 元数据过滤 | фільтрація за метаданими | |
| 父子块检索 | parent-child retrieval | усталене |
| 引用 | цитування джерел | |
| 语义 | семантичний | |
| 知识库 | база знань | |
| 评测集 | набір для оцінювання | |
| Bad Case | проблемний кейс | |

Без перекладу: `embedding`, `RAG`, `Agentic RAG`, `GraphRAG`, `ANN`, `HNSW`, `BM25`,
`MRR`, `NDCG`, `RAGAS`, `Query Rewriting`, `Multi-Query`.

## Агенти

| Оригінал | Українською | Нотатки |
|---|---|---|
| 智能体 / Agent | агент | |
| 工具调用 | виклик інструментів, tool calling | |
| 工具契约 | контракт інструмента | |
| 规划 | планування | |
| 记忆 | пам'ять | 短期/长期记忆 → коротко-/довгострокова пам'ять |
| 状态机 | скінченний автомат | |
| 权限 | права доступу, дозволи | 权限越界 → вихід за межі дозволів |
| 死循环 | нескінченний цикл | |
| 兜底 | запасний сценарій, fallback | |
| 失败恢复 | відновлення після збою | |
| 任务完成率 | частка виконаних задач | |
| 上下文污染 | забруднення контексту | |
| 编排 | оркестрація | |
| 多智能体 | мультиагентна система | |

Без перекладу: `ReAct`, `Reflection`, `Plan-and-Execute`, `DAG`, `Checkpoint`, `Trace`,
`MCP`, `Function Calling`, `Harness`, `Planner/Worker/Reviewer`, `Skills`, `Hooks`.

## Донавчання і вирівнювання

| Оригінал | Українською | Нотатки |
|---|---|---|
| 微调 | fine-tuning, донавчання | |
| 全参微调 | повне донавчання | |
| 对齐 | вирівнювання (alignment) | |
| 偏好对齐 | вирівнювання за вподобаннями | |
| 蒸馏 | дистиляція | 硬蒸/软蒸 → жорстка / м'яка |
| 低秩 | низькоранговий | |
| 秩 (rank) | ранг | |
| 灾难性遗忘 | катастрофічне забування | |
| 数据标注 | розмітка даних | |
| 过拟合 | перенавчання (overfitting) | **не** плутати з 训练 |

Без перекладу: `SFT`, `RLHF`, `DPO`, `LoRA`, `QLoRA`, `PEFT`, `reward model`.

## Інференс, деплой, продуктивність

| Оригінал | Українською | Нотатки |
|---|---|---|
| 部署 | розгортання, деплой | |
| 自部署 | самостійний хостинг | |
| 量化 | квантизація | |
| 显存 | відеопам'ять, VRAM | |
| 吞吐 | пропускна здатність | |
| 延迟 | затримка | |
| 压测 | навантажувальне тестування | |
| 容量规划 | планування потужностей | |
| 限流 | обмеження частоти запитів | rate limiting |
| 熔断 | запобіжник (circuit breaker) | |
| 重试 | повторна спроба | |
| 超时 | таймаут | |
| 缓存 | кеш, кешування | |
| 灰度发布 | поетапне викочування | canary release |
| 监控 | моніторинг | |
| 流式输出 | стримінг, потокова відповідь | |
| 并发 | конкурентність | |

Без перекладу: `vLLM`, `SGLang`, `KV Cache`, `PagedAttention`, `Prefix Cache`, `TTFT`,
`TPOT`, `P99`, `Goodput`, `FP8`, `INT8`, `AWQ`, `GPTQ`, `GGUF`, `W4A16`.

## Transformer

| Оригінал | Українською | Нотатки |
|---|---|---|
| 注意力机制 | механізм уваги (attention) | |
| 自注意力 | self-attention | |
| 多头注意力 | multi-head attention | |
| 位置编码 | позиційне кодування | |
| 残差连接 | залишкове з'єднання | residual connection |
| 归一化 | нормалізація | LayerNorm — не перекладено |
| 前馈网络 | мережа прямого поширення (FFN) | |
| 编码器 / 解码器 | енкодер / декодер | |
| 掩码 | маска, маскування | |
| 手撕 | «пишемо з нуля» | ідіома: реалізувати без фреймворків |
| 矩阵 / 转置 | матриця / транспонування | |
| 点积 | скалярний добуток | |

Без перекладу: `Q/K/V`, `Softmax`, `LayerNorm`, `RMSNorm`, `RoPE`, `GQA`, `MQA`, `logits`.

## Кар'єра і співбесіди

| Оригінал | Українською | Нотатки |
|---|---|---|
| 面试 | співбесіда | |
| 面经 | розбір реальної співбесіди | |
| 面试官 | інтерв'юер | |
| 高频面试题 | типові питання на співбесіді | |
| 岗位 | позиція, вакансія | |
| 简历 | резюме | |
| 算法岗 | позиція ML-інженера / дослідника | не «алгоритмічна» |
| 应届生 / 实习 | випускник / стажування | |
| 大厂 | великі технологічні компанії | не «великі заводи» |
| 技术选型 | вибір технологій | |
| 取舍 | компроміси, trade-off | |
| 落地 | впровадження у продакшн | 生产实践 → продакшн-практика |
| 项目 | проєкт | саме «проєкт», не «проект» |

---

## Стиль

- **Звертання:** «ви» — однаково в усьому репозиторії.
- **Правопис:** чинний український правопис — «проєкт», «матеріал», «в інтернеті».
- **Китайський синтаксис не скальковано.** Оригінал любить «我们来看一下» — це не
  «давайте подивимось», а просто «розгляньмо» або взагалі без вступу.
- **Риторичні питання в заголовках збережено** — це фішка оригіналу
  («Навіщо потрібен RAG, якщо є LLM?»).
- **Від себе в текст нічого не дописано.** Явні числові помилки оригіналу виправлено,
  і кожне виправлення зафіксовано в `NOTES.md`; змістові й стилістичні хиби лишено
  як є, а зауваження до них — теж у `NOTES.md`. Власні матеріали форку винесено
  в окремі статті з явною позначкою на початку.

## Матеріали з терміном придатності

Статті з конкретними цінами, версіями моделей чи витоками промптів мають
одразу після заголовка врізку:

```markdown
> **Станом на вересень 2026.** Ціни, версії моделей і назви продуктів у цій статті
> швидко змінюються — перед використанням звіряйтеся з офіційною документацією.
```

Формулювання однакове для всіх таких файлів, щоб його можна було знайти й оновити
одним `grep`. Самих цифр усередині статей не вигадувано й не «освіжувано» — вони
лишилися такими, як в оригіналі, бо це фіксація моменту.
