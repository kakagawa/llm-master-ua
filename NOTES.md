# Нотатки редактора

Зауваження до оригіналу та рішення, ухвалені під час адаптації.
Правило: текст перекладаємо чесно, а всі претензії до оригіналу — сюди.

## Помилки й прогалини оригіналу

- `docs/llm/app/why_rag.md` — заголовок розділу 4 обіцяє «де RAG доречний, **а де ні**»,
  але в тексті перелічено лише доречні сценарії. Нерелевантні згадані аж у розділі 5,
  у відповіді на питання зі співбесіди. Переклав як є. Варто дописати свій абзац.
- `docs/README.md`, `docs/llm/app/structured_output.md`, `model_integration.md` —
  посилання на `.html` замість `.md`. Помилка вже в оригіналі.

## Адаптації, а не дослівний переклад

- `docs/llm/transformer/qkv.md` — приклад з увагою побудований на розборі китайського
  слова 少女 по окремих ієрогліфах: токеном виступає 女 всередині слова. Українською
  слово так не розпадається, тому токеном зробив «дівчина», а означення лишив навколо.
  Суть прикладу (означення важать більше за службові слова) збережена.
- Там же: імена 小明 / 小刚 у прикладі на розв'язання займенника замінені на Петра та
  Івана. Приклад про неоднозначність «він» — локалізація тут обов'язкова.
- Вступи й кінцівки в дусі «підпишіться на канал», «читачі публічки вже знають»
  замінені на нейтральні відсилання до сусідніх статей репозиторію.

## Термінологічні рішення поза глосарієм

- 检索增强生成 → «генерація з доповненням пошуком» при розшифровці абревіатури,
  далі всюди просто `RAG`.
- 上下文稀释 → «увага розмивається» (про довгий контекст), не «розрідження».

## Знята китайська специфіка

- `docs/llm/intro/ai-coding-three-layers.md` — оригінал наскрізь особистий: звертання
  до аудиторії публічки («录友»), посилання на mp.weixin.qq.com, історія про бани
  двох акаунтів Claude, платна спільнота 知识星球, GLM-5.1 як замінник Claude через
  недоступність в Китаї. Технічний аргумент (три рівні: модель / ядро агента / оболонка)
  збережено повністю, особисто-китайське обрамлення замінено на нейтральне.
  Приклад-діалог «веб чи CLI?» збережено — він несе думку.
- `docs/llm/intro/why_hello_costs_tokens.md` — прибрано зав'язку про бани акаунтів
  і рекламу проксі-сервісу apidock.ai (обхід регіональних обмежень, нам не актуально).
  Усі реальні цифри з панелі тарифікації та обидві схеми збережено — це суть статті.
  Биті лінки на видалені заглушки перенаправлено в `../claude/`, а не викинуто.
- `docs/llm/intro/application_development.md` — прибрано двічі повторену рекламу
  платного курсу (посилання на WeChat) і прив'язку до китайського джоб-сайту та
  Шеньчженя. Аналогія «двигун / автопілот / збирання авто» і всі 6 питань збережені.
- `docs/llm/intro/llm_keywords.md` — 6 зовнішніх посилань на WeChat замінено на
  внутрішні статті репозиторію (harness_interview, rag_interview, embedding,
  graphrag_interview, agent_interview, claude_code_deep_dive). Навігація стала
  замкненою всередині репозиторію — це краще за оригінал.
  Приклади локалізовано: 张三/28 → Іван Петренко/28; «погода в Пекіні» → у Києві.
  Конкретні розміри контекстних вікон прибрано з тексту: цифри застарівають за місяці,
  а сенс абзацу («вікно швидко заповнюється») від них не залежить.
- `docs/llm/intro/llm_pricing.md` — вирізано 8 реферальних посилань автора на bigmodel.cn
  (`?icode=...`): це монетизація, а не зміст. Назва моделі лишилась текстом.
  Ціни в юанях збережено як є — це фіксація моменту, вигадувати конверсію не можна.
  Додано врізку «Станом на вересень 2026».
  Вимірювання токенів зроблені для китайської; додано явне застереження, що для
  кирилиці картина інша й коефіцієнти треба міряти своїм текстом, а не вгадувати.
  **TODO:** написати власний розділ із реальними вимірами для української —
  це найцінніше, чого немає ні в оригіналі, ні деінде українською.
- `docs/llm/intro/model_distillation.md` — прибрано вступ через платну спільноту
  автора (знімок екрана з приватного питання) і згадку відеоінтерв'ю, за яким автор
  вигадав терміни «жорстка / м'яка дистиляція». Самі терміни збережено, бо на них
  тримається структура статті. Усі публічні джерела (умови OpenAI, довідка Anthropic,
  TechCrunch про свідчення Маска) збережено з посиланнями — це фактура, не промо.

## Конвертація валют

- `docs/llm/intro/llm_pricing.md` — усі тарифи GLM переведено з юанів у долари США за
  курсом **¥1 ≈ $0,14**. Курс не вигаданий: його використовує сам автор у розділі 7,
  де порівнює три моделі. Формули переписані одразу в доларах, а не перераховані
  з юаневих результатів.
- Заодно усунуто внутрішню суперечність оригіналу: у розділі 7 автор рахував за трохи
  різними курсами (~$0,83 при 0,1389 і ~$3,43 при 0,1429), тож таблиця не сходилася
  з розділом 2. Тепер усюди єдиний курс 0,14.
- Економія від кешу в розділі 5 після конвертації дає 54%, а не 53% як в оригіналі —
  чистий артефакт округлення. Поставлено цифру, що збігається з показаними сумами.
- Решта тверджень перевірена скриптом і зійшлася: 44% економії в розділі 6,
  ~$50 за тисячу запитів на день, різниця Opus/GLM майже вдесятеро.

## Transformer: адаптації прикладів

- `transformer_data_flow.md` — приклад «远方有颗苹果树» з розбиттям на 7 ієрогліфів
  замінено на «Удалині росте яблуня». Кількість токенів (7) збережена, бо від неї
  залежить уся подальша математика статті (матриці 7×512, 7×2048). Оригінал сам
  зазначає, що розбиття залежить від токенізатора, тож точний перелік токенів
  не наводжу — це було б вигадуванням.
- `transformer_data_flow.md` — «你打我 / 我打你» → «ти вдарив мене / я вдарив тебе».
  Приклад про залежність змісту від позиції українською працює навіть краще.
- `mha.md` — приклад із 麻辣烫 (китайська страва) замінено на борщ. Усі чотири типи
  зв'язків (вказівка, означення, інструмент, підмет) збережено один в один.
- `qkv_cal.md` — приклад «我 爱 你» → «я люблю тебе». Три токени збережено, тож уся
  ручна арифметика (матриці 3×3, ваги 0.40/0.20/0.40 і 0.27/0.27/0.46, вихід [0.73, 0.73])
  лишається правильною без жодної зміни чисел. Підписи рядків і стовпців у таблицях
  перейменовано відповідно.
- `pos_encode.md` — «我打了他 / 他打了我» → «я вдарив його / він вдарив мене».

## Виправлені помилки в обчисленнях оригіналу

- `docs/llm/transformer/qkv_cal.md` — **арифметична помилка в оригіналі.**
  Третій рядок матриці ваг уваги подано як `[0.27, 0.27, 0.46]`, а вихід — `[0.73, 0.73]`.
  Правильно: `e^1.41 / (2·e^0.71 + e^1.41)` = 0.5035, тобто `[0.25, 0.25, 0.50]`,
  а вихід `[0.75, 0.75]`. Перші два рядки (`0.40/0.20/0.40` і `0.20/0.40/0.40`)
  в оригіналі правильні — помилка лише в третьому.
  Виправлено, бо це покроковий приклад для навчання: читач перераховує його сам
  і неминуче впреться в розбіжність. Заразом формулювання стало чистішим —
  0.50 проти 0.25 це рівно вдвічі, а не «помітно більше».
  Перевірку зафіксовано скриптом `tools/verify-attention.mjs`.

**Правило:** будь-який покроковий числовий приклад перед публікацією перераховуємо
скриптом, а не звіряємо очима.

- `docs/llm/transformer/attention_code.md` — **вигадані числові виходи в оригіналі.**
  Стаття наводить результати запуску коду з `np.random.seed(42)`. Справжній там лише
  перший вивід (матриця X). Усі подальші — оцінки уваги, діапазони значень, ваги,
  підсумковий вихід — не відповідають тому, що видає код. Ознака підробки помітна
  неозброєним оком: числа перевикористовують ті самі цифри в різному порядку
  (`0.18745632`, `0.28745632`, `0.39482749`).

  Реальні значення: діапазон оцінок до масштабування не «-4.23 .. 3.87»,
  а `-17.4478 .. 14.7643`; ваги першого токена не `[0.087, 0.215, ...]`,
  а `[0.04115936, 0.01223386, 0.17820851, 0.16625512, 0.21927912, 0.14523322, 0.23763083]`.

  Усі виводи замінено на справжні. Щоб їх обчислити без numpy, довелося відтворити
  legacy-генератор numpy (MT19937 + polar-метод) у `tools/numpy-rng.mjs` та повторити
  форматування масивів numpy. Коректність підтверджена контрольно: наш вивід матриці X
  збігається з оригінальним байт у байт, включно зі скороченням `-0.1382643 `.
  Перевірка: `node tools/verify-attention-code.mjs`.

  Рядок `sentence = "远方有颗苹果树"` замінено на українське речення. Це рядкова
  константа, яка ніде не використовується в обчисленнях, тож на результат не впливає.
- `docs/llm/transformer/fnn_code.md` — ще одне вигадане число. Стаття стверджує,
  що до ReLU у `hidden` було «112 від'ємних значень». Насправді **100**.
  Схоже, автор просто взяв половину від 224 (7 × 32). Виправлено; перевірка —
  `node tools/verify-ffn.mjs`.
  Твердження про кількість параметрів (FFN удвічі більше за attention, ~2/3 від суми)
  перевірені й правильні: 4·d² = 256 проти 2·d·d_ff = 512, частка 66,7%.
- `docs/llm/transformer/mha_code.md` — чистий: друкує лише розмірності тензорів,
  числових значень немає, всі shape по ланцюжку сходяться.
- `docs/llm/transformer/layernorm_residual_code.md` — третя вигадка. Показники
  «до нормалізації» подані як середнє -0.37 і std 9.87. Насправді **-1.69 і 9.13**.
  Показники «після» (0.0000 / 1.0000) правильні, але це й не дивно — вони випливають
  із самої математики LayerNorm, а не з конкретних даних. Уточнено й знак нуля:
  сире значення -3.569e-17, тож Python друкує `-0.0000`, а не `0.0000`.
  Перевірка — `node tools/verify-layernorm.mjs`.
- `docs/llm/transformer/transformer_block_code.md`, `tiny_transformer_code.md` — чисті.
  Перший друкує лише shape. У другому є справжнє передбачення `[5 5 4 3]`, і повний
  незалежний прогін (`tools/verify-tiny-transformer.mjs`) дає **точно те саме**.
  Отже, цей приклад автор справді запускав. Заразом це незалежне підтвердження, що
  відтворення numpy RNG у `tools/numpy-rng.mjs` коректне.

### Підсумок перевірки коду Transformer

Прогнано 6 файлів із кодом. Вигадані числа знайдено у 3 з них.
Закономірність: підроблялося саме те, що **не випливає з математики**.
Там, де результат вимушений теорією (суми softmax = 1, нуль від'ємних після ReLU,
середнє 0 і дисперсія 1 після LayerNorm, незмінність shape), усе правильно.
Там, де треба було справді запустити код і переписати вивід, — вигадано.
- `docs/llm/app/embedding.md` — стаття наскрізь побудована навколо китайської мови.
  Технічну частину (bi-encoder проти cross-encoder, розмірності, ліміт токенів,
  асиметричний пошук) збережено як є. Розділ про вибір моделі під мову позначено
  врізкою: логіка та сама й для української, але **конкретних рейтингових позицій
  для української не вигадую** — вказано дивитися багатомовний MTEB і міряти
  на власних документах. Китайські моделі лишені як приклад методики, не як порада.
  Діалог зі вступу локалізовано вдало: 民用航空器 проти 飞机 → «повітряне судно»
  проти «літак». Приклад про межі ключового пошуку працює один в один.
- `docs/llm/app/rag_evaluation.md` — усі шість метричних прикладів (Recall 2/3,
  Precision 2/5, MRR, Faithfulness 2/3, Context Relevancy 1/4, Context Recall 1/2)
  і розрахунок частки прийняття перевірені й **правильні**.

  Виправлено одне: кратність вартості оцінювання. В оригіналі в тексті сказано
  «у 4–8 разів більше за питання-відповідь», а в Q7 тієї самої статті — «у 5–8 разів».
  За наведеними ж там числами (700 токенів на QA проти 4000–8000 на чотири метрики
  RAGAS) виходить **5,7–11,4×**. Схоже, автор переплутав абсолютне число токенів
  (4000–8000) із кратністю. Зведено до «приблизно в 6–11 разів» в обох місцях.

  Приклад Context Relevancy переструктуровано: в оригіналі чанк складається з трьох
  речень, але рахується як 1/4. Щоб арифметика була однозначною, розписав його на
  чотири окремі речення, з яких релевантне рівно одне. Результат 25% збережено.
  Локалізовано 五险一金 (китайський обов'язковий соцпакет) → медичне страхування.
- `docs/llm/app/plan_execute_dag.md` — твердження «6 кроків насправді 3 рівні» перевірено
  прогоном алгоритму Кана на графі зі статті: рівні виходять [1,2,3,4] / [5] / [6],
  циклів немає. Збігається точно.
  У JSON-прикладі DAG замінено `"params": {...}` на `"params": {}` — трикрапка робила
  блок невалідним JSON, а стаття саме про те, що план має бути машинно розбірним.
- `docs/llm/app/lora_qlora.md` — числа перевірені прогоном:
  ваги 7B FP16 = 14 ГБ ✓, градієнти 14 ГБ ✓, Adam FP16 = 28 ГБ ✓,
  квантизація 14 → 3,5 ГБ ✓, три моделі 3 × 14 = 42 ГБ ✓.
  Співвідношення параметрів: 4096² / (2 × 4096 × 8) = **рівно 256×**
  (в оригіналі «понад 250 разів» — правильно, уточнено до точного значення).

  Виправлено одне: сума відеопам'яті. За власними числами статті
  14 + 14 + 28 = **56 ГБ**, а написано «наближається до 70 ГБ».
  Замінено на «приблизно 56 ГБ, а зі станом оптимізатора у FP32 — за 80 ГБ»,
  що узгоджується і з арифметикою, і з наступним реченням оригіналу
  про FP32 як типовий варіант.

## `app/finetuning_interview.md`

- Прибрано посилання на платну колонку з резюме (`../../jianli/llm/llm_8.md`) — цієї теки у форку немає. Сам список чотирьох складників опису проєкту в резюме збережено повністю, він самодостатній.
- Прибрано звертання «录友» (учасники платної спільноти автора) — замінено на нейтральне.
- Коментарі `<!-- drawio源文件 -->` прибрано, як і в решті перекладених статей: вихідних `.drawio` у репозиторії немає.

## Блок про розгортання (`deployment_options`, `model_quantization`, `kv_cache_paged_attention`, `stress_testing`)

Усі числові приклади перевірено вручну — помилок в оригіналі не знайдено:

- KV-кеш на токен: `2 × 32 шари × 8 KV-голів × 128 × 2 байти = 131072 Б = 128 КіБ` — точно.
  Похідні: 8K токенів = рівно 1 ГіБ, 32K = 4 ГіБ, 128K = 16 ГіБ, 8 запитів по 32K = 32 ГіБ. Усе сходиться.
- Ваги моделі на 70 млрд: BF16 = 140 ГБ, INT8 = 70 ГБ, INT4 = 35 ГБ — точно.
- Goodput: 20 зап/с × 70 % = 14 зап/с; 22 зап/с × 40 % = 8,8 зап/с — точно.

Датування: у `deployment_options` і `model_quantization` додано банер «станом на вересень 2026»,
бо перелік і статуси рушіїв inference швидко змінюються. Внутрішні звірки оригіналу
(«липень 2026», «серпень 2026») збережено як є — це дати перевірки джерел, а не публікації.

## `claude/ai_code_migration.md`

Цифра «≈165 тис. доларів» за 5,9 млрд некешованих вхідних і 690 млн вихідних токенів
не відтворюється з відкритих тарифів однозначно (за Opus-цінами $15/$75 за млн виходить
≈140 тис.). Лишив як в оригіналі — це посилання на внутрішні дані Anthropic, а не
власний розрахунок автора статті. Долари в оригіналі вже були доларами, конвертація не потрібна.

## Розділ `claude/`

- Посилання на офіційну документацію Claude Code переведено з `/docs/zh-CN/` на `/docs/en/`:
  україномовної версії немає, а китайська для читача форку марна.
- У `claude_code_400k_sessions.md` оригінал містив три коментарі-заглушки
  «卡通图待生成» (мультяшна ілюстрація в роботі) з підписами під неіснуючі картинки.
  Заглушки прибрано, а зміст підписів вплетено в текст звичайними реченнями —
  щоб не лишати в перекладі посилань на те, чого немає.
- Звертання «录友» (учасники платної спільноти автора) прибрано в усіх статтях розділу.

## `interview/llm/agent_hybrid_routing_interview.md`

Виправлено арифметику в порівнянні каскаду з маршрутизацією. В оригіналі:
«одна модель — 100, каскад — 10 + 10 + 30 = 50, ідеал — 40; каскад економить 50 %,
але на 25 % дорожчий за ідеал». Ці числа не сходяться з власним твердженням статті,
що мала модель дешевша **в 10 разів**: 70 простих запитів через малу коштують 7, а не 10,
а 30 складних через малу — 3, а не 10.

Перерахував за коефіцієнтом 10× (перевірено скриптом):

- одна велика модель: 100 × 1 = **100**
- каскад: 70 × 0,1 + 30 × (0,1 + 1) = 7 + 33 = **40**
- ідеальна маршрутизація: 70 × 0,1 + 30 × 1 = **37**

Тобто каскад дешевший за «всім однаково» на **60 %** і дорожчий за ідеал на **~8 %**.
Висновок статті (каскад економить, але програє маршрутизації через марний прогін
складних запитів) лишився той самий — змінилися лише числа, що його підпирають.

## `interview/llm/graphrag_interview.md`

У списку джерел оригінал підписував статтю про LightRAG як «Luo Z, Song K 等».
Це не збігається з авторством статті arXiv:2410.05796, тож ініціали прибрано —
лишилися назва статті й посилання. Для GraphRAG (Edge D., Trinh H.) авторство
збігається, тож там лишив як є.
